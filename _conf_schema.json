{
  "github_token": {
    "type": "string",
    "description": "GitHub API Token",
    "default": "",
    "hint": "如果你的仓库很多，建议你在这里配置一个 GitHub API Token，以提高API调用的限制。"
  },
  "repositories": {
    "type": "list",
    "description": "要监控的仓库列表，支持两种格式：1.字符串格式 2.字典格式 ",
    "default": [],
    "hint": "例如:1592363624/astrbot_plugin_github_monitor_shell|123456|91219736 或 {\"owner\": \"1592363624\", \"repo\": \"astrbot_plugin_github_monitor_shell\", \"groups\": [\"123456\", \"91219736\"]}。群号也支持 UMO 格式（平台ID:消息类型:会话ID）"
  },
  "check_interval": {
    "type": "int",
    "description": "检查间隔（分钟）",
    "default": 60
  },
  "notification_targets": {
    "type": "list",
    "description": "通知目标（私聊）",
    "default": [],
    "hint": "支持两种格式：1.纯数字QQ号，如 123456，自动匹配QQ平台；2.UMO 格式：平台ID:消息类型:会话ID，如 qq_official:FriendMessage:0771687B325FC423AD9F4C06A88D84E3（推荐，适配QQ官方机器人等非数字会话ID及多bot场景）。UMO 可通过让 bot 收到一条消息后在 WebUI 对话列表/日志中查看"
  },
  "group_notification_targets": {
    "type": "list",
    "description": "全局群通知目标",
    "default": [],
    "hint": "所有仓库更新时都会通知这些群组，如果需要为特定仓库配置额外的群组，请在仓库配置中指定。支持两种格式：1.纯数字群号，如 123456；2.UMO 格式：平台ID:GroupMessage:会话ID，如 aiocqhttp:GroupMessage:123456 或 小爱同学:GroupMessage:0771687B325FC423AD9F4C06A88D84E3（推荐，适配QQ官方机器人的十六进制openid及多bot场景）"
  },
  "platform_id": {
    "type": "string",
    "description": "指定平台ID（高级选项，留空则自动匹配）",
    "default": "",
    "hint": "仅当配置了多个同类型平台时需要手动指定，通常留空即可。留空时会按 aiocqhttp → qq_official → qq_official_webhook 的优先级自动检测可用的QQ平台。使用 UMO 格式的推送目标时可忽略此项（UMO 自带平台路由信息）"
  },
  "time_zone": {
    "type": "string",
    "description": "时间显示时区（IANA 标准名称）",
    "default": "Asia/Shanghai",
    "hint": "例如: Asia/Shanghai, UTC"
  },
  "time_format": {
    "type": "string",
    "description": "时间显示格式（Python strftime）",
    "default": "%Y-%m-%d %H:%M:%S",
    "hint": "例如: %Y-%m-%d %H:%M:%S"
  },
  "issue_monitor_enabled": {
    "type": "bool",
    "description": "是否监控项目仓库的 Issues 动态",
    "default": true,
    "hint": "开启后，随每次仓库检查一同检测上方配置的监控仓库的 Issues：有新增 Issue、或 Issue 下出现新评论（讨论变化）时立即推送通知，推送目标与 Commit 通知一致"
  },
  "issues_cron_enabled": {
    "type": "bool",
    "description": "是否启用 Issues 定时推送",
    "default": false,
    "hint": "开启后，将按照 cron 表达式自动查询所有仓库的 open issues 并私聊发送给通知目标"
  },
  "issues_cron_expression": {
    "type": "string",
    "description": "Issues 定时推送的 Cron 表达式",
    "default": "0 9 * * *",
    "hint": "标准 5 段 Cron 表达式（分 时 日 月 周），例如: 0 9 * * * 表示每天 9:00 推送，0 9 * * 1-5 表示工作日 9:00 推送"
  },
  "issues_push_min_interval": {
    "type": "int",
    "description": "Issues 推送最小间隔（分钟）",
    "default": 60,
    "hint": "相同内容的推送不会在此间隔内重复发送，防止短时间内重复推送"
  },
  "max_comment_length": {
    "type": "int",
    "description": "Issue 评论内容最大长度（字符）",
    "default": 400,
    "hint": "Issues 动态通知中每条评论正文的最大长度，超出部分截断并附提示，防止评论过长导致 QQ 消息发送失败"
  },
  "message_max_length": {
    "type": "int",
    "description": "单条消息安全长度（字符）",
    "default": 2000,
    "hint": "超过该长度的纯文本通知会自动拆分为多条消息依次发送（QQ 对单条消息长度有限制，过长会导致整条发送失败）。推送通知与命令查询回复均生效"
  },
  "image_output": {
    "type": "object",
    "description": "文转图（图片通知）",
    "hint": "将 commit 更新通知渲染为图片卡片发送，依赖 AstrBot 的文转图（T2I）服务",
    "items": {
      "commit_output_format": {
        "type": "string",
        "description": "Commit 通知输出格式",
        "options": ["text", "image"],
        "default": "text",
        "hint": "text 为纯文本通知；image 会将 commit 更新渲染为图片卡片发送，渲染或发送失败的通知会进入重试队列自动重试"
      },
      "commit_image_template": {
        "type": "string",
        "description": "Commit 卡片图片模板",
        "options": ["random", "terminal", "github_dark", "light", "retro_term", "miku", "sakura"],
        "default": "terminal",
        "hint": "内置主题：terminal 终端风 / github_dark GitHub暗色 / light 简洁浅色 / retro_term 复古绿屏 / miku 初音风 / sakura 樱花粉；选 random 则每次推送随机使用一套。也可以在插件数据目录的 templates/ 下放置自定义 .html 模板，文件名（不含后缀）即模板名，直接填在这里即可使用（自定义模板也会进入 random 的随机池）"
      },
      "enable_base64_image": {
        "type": "bool",
        "description": "图片传输使用 Base64 编码",
        "default": true,
        "hint": "启用后图片以 Base64 编码传输；关闭后将传递本地临时文件路径给平台适配器。某些 OneBot 实现端与 AstrBot 分离部署时需要开启"
      },
      "t2i_image_type": {
        "type": "string",
        "description": "图片格式",
        "options": ["png", "jpeg"],
        "default": "png",
        "hint": "PNG 无损但体积较大；JPEG 体积较小"
      },
      "t2i_quality": {
        "type": "int",
        "description": "图片质量（仅 JPEG 生效）",
        "default": 90,
        "slider": {"min": 1, "max": 100, "step": 1}
      },
      "t2i_scale": {
        "type": "string",
        "description": "图片清晰度等级",
        "options": ["normal", "high", "ultra"],
        "default": "high",
        "hint": "normal=1.0x, high=1.3x, ultra=1.8x。等级越高图片越清晰，渲染越慢"
      },
      "enable_group_file_upload": {
        "type": "bool",
        "description": "上传图片到群文件",
        "default": false,
        "hint": "开启后，图片通知发送成功时会将图片备份上传到群文件（仅 OneBot/aiocqhttp 平台的 QQ 群生效）"
      },
      "group_file_folder": {
        "type": "string",
        "description": "群文件目标文件夹名",
        "default": "",
        "hint": "留空则上传到群文件根目录；填写文件夹名时会自动查找，不存在则创建"
      },
      "enable_group_album_upload": {
        "type": "bool",
        "description": "上传图片到群相册",
        "default": false,
        "hint": "此功能为 NapCat 扩展 API，其他 OneBot 实现可能不支持"
      },
      "group_album_name": {
        "type": "string",
        "description": "群相册名称",
        "default": "",
        "hint": "上传到指定名称的相册；留空时使用相册列表中的第一个"
      },
      "group_album_strict_mode": {
        "type": "bool",
        "description": "群相册严格模式",
        "default": true,
        "hint": "开启后，找不到指定名称的相册时放弃上传，防止误传到默认相册；关闭后回退到相册列表第一个"
      }
    }
  },
  "forward_merge": {
    "type": "object",
    "description": "合并转发推送（Commit）",
    "hint": "开启后，插件仍按原方式定时轮询监听仓库，但本轮检测到的 commit 通知不再逐条发送，而是先收集起来，最后整合成一条「合并转发」聊天记录发送到配置的群/私聊。合并转发仅 aiocqhttp（OneBot v11，如 NapCat/Lagrange）平台支持，其他平台会按配置自动降级为逐条普通消息。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "以合并转发形式发送 Commit 通知",
        "default": false,
        "hint": "开启后，每次轮询中所有仓库检测到的 commit 通知会合并为一条转发聊天记录；关闭则维持原本的逐条发送行为"
      },
      "content_mode": {
        "type": "string",
        "description": "合并转发节点内容形式",
        "options": ["follow", "text", "image"],
        "default": "follow",
        "hint": "follow：跟随「文转图（图片通知）」分组里的 Commit 通知输出格式（text→文字节点，image→图片卡片节点）；text：节点一律使用文字；image：节点一律使用图片卡片（需先配置好 T2I 文转图服务，单个节点渲染失败时自动回退为文字节点）"
      },
      "node_name": {
        "type": "string",
        "description": "转发节点显示昵称",
        "default": "GitHub监控",
        "hint": "合并转发记录中每个节点的发送者昵称，仅用于展示"
      },
      "node_uin": {
        "type": "string",
        "description": "转发节点显示账号",
        "default": "10000",
        "hint": "合并转发记录中每个节点的发送者账号（uin），仅用于展示，建议填写机器人自身的QQ号"
      },
      "max_nodes_per_message": {
        "type": "int",
        "description": "单条合并转发的最大节点数",
        "default": 20,
        "hint": "本轮收集到的 commit 记录超过该数量时会拆分为多条合并转发依次发送（QQ 对单条转发记录的节点数有限制）",
        "slider": {"min": 1, "max": 100, "step": 1}
      },
      "fallback_to_normal": {
        "type": "bool",
        "description": "平台不支持合并转发时降级为普通消息",
        "default": true,
        "hint": "开启后，向 qq_official / Telegram 等不支持合并转发的平台推送时会自动回退为逐条普通消息，保证不漏通知；关闭则这些目标会被标记为发送失败并进入重试队列"
      }
    }
  }
}
